/**
 * AdminLunchSpecials.tsx
 * Controls: /admin/lunch-specials — the specials manager scoped to lunch
 * specials only (days, start/end time, enable/disable).
 */
import AdminSpecials from "./AdminSpecials";

const AdminLunchSpecials = () => <AdminSpecials focus="lunch" />;

export default AdminLunchSpecials;
