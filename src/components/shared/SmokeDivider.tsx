/**
 * SmokeDivider.tsx (shared)
 * Controls: the smoke/gold divider between sections.
 * Re-exported here so all shared building blocks live in one folder.
 */
export { SmokeDivider } from "@/components/retina/SmokeDivider";
